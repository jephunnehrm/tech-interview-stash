[
  {
    "id": "e-dev-001",
    "role": "Developer",
    "problem": "A nightly batch job that used to finish in 20 minutes now takes over 4 hours after the input table grew from 2M to 40M rows. No code changes were made recently. How do you find the bottleneck and fix it?",
    "solution": "Start by capturing an actual execution plan for the job's core query rather than guessing — row-count growth alone often exposes a missing or non-selective index that was never a problem at small scale.\n\nCheck whether an index that used to be used got dropped implicitly (e.g. by a migration), whether statistics are stale after the growth, and whether the query does row-by-row processing (a cursor or N+1 pattern) that scales linearly with row count instead of set-based operations that scale better.\n\nFix in order of cheapest-to-riskiest: update statistics, add/restore the missing index, rewrite row-by-row logic as set-based SQL, then consider batching or partitioning only if the first three don't close the gap."
  },
  {
    "id": "e-dev-002",
    "role": "Developer",
    "problem": "You're asked to add a new required field to an API response that's consumed by three different mobile app versions still in production, two of which you can't force-update.",
    "solution": "Treat this as a backward-compatibility problem, not just a schema change. Add the field as optional/nullable in the response rather than required, so older clients that ignore unknown fields keep working unchanged.\n\nIf the field genuinely must be required for new behavior, version the endpoint (or negotiate via an `Accept` header/API version) so old clients keep hitting the old contract while new clients opt into the new one. Avoid breaking the existing contract in place — that punishes every client you don't control."
  },
  {
    "id": "e-lead-001",
    "role": "Tech Lead",
    "problem": "Two senior engineers on your team disagree on whether to introduce a new ORM for a greenfield service — one wants to move fast with familiar tooling, the other is concerned about long-term query performance control. The disagreement is stalling the sprint.",
    "solution": "Reframe the discussion around evidence rather than preference: define the 2-3 concrete scenarios the service actually needs to handle well (expected query patterns, write volume, reporting needs), and have each engineer spend a timeboxed half-day building a small spike against those scenarios with their preferred approach.\n\nAs tech lead, your job is to make the decision timely, not to let it resolve itself by attrition — set a decision deadline up front so the spike informs the call rather than becoming its own open-ended debate. Document the decision and the reasoning (a lightweight ADR) so it doesn't get relitigated every sprint."
  },
  {
    "id": "e-lead-002",
    "role": "Tech Lead",
    "problem": "Your team's velocity has visibly dropped over the last two sprints, and leadership is asking why. Digging in, you find the drop tracks with a rise in production incidents from a service the team inherited six months ago with almost no tests.",
    "solution": "Surface the real tradeoff to leadership explicitly rather than absorbing it silently as \"the team is slower\": the team is spending an increasing share of capacity on unplanned incident response for an under-tested inherited service, which is capacity not visible in the sprint board.\n\nPropose a bounded remediation plan — characterization tests around the highest-incident code paths first, not a full rewrite — with a concrete time-box, and negotiate temporarily reduced sprint commitments to fund it. Making the tradeoff visible and giving leadership a plan with an end date is more useful than either quietly grinding through it or escalating without a proposed fix."
  },
  {
    "id": "e-arch-001",
    "role": "Architect",
    "problem": "A product line is expanding from a single-tenant deployment per customer to a multi-tenant SaaS model. You need to decide on a tenant isolation strategy for the database layer.",
    "solution": "Lay out the three standard options against the product's actual constraints rather than defaulting to the trendiest one: separate database per tenant (strongest isolation and easiest compliance story, highest operational overhead at scale), separate schema per tenant (middling isolation, still one database to operate), and shared schema with a tenant-id column on every table (cheapest to run, weakest isolation, requires rigorous enforcement of row-level filtering everywhere).\n\nThe deciding factors are usually: expected tenant count and their size variance, any regulatory/data-residency requirements that demand hard isolation, and how much engineering discipline you can guarantee around never leaking a query without a tenant filter. For most SaaS products with a large number of small-to-medium tenants, shared schema plus an enforced tenant-id (ideally backed by database-level row-level security, not just application code) is the pragmatic default; per-tenant databases are reserved for customers with contractual isolation requirements."
  },
  {
    "id": "e-arch-002",
    "role": "Architect",
    "problem": "The company wants to break apart a five-year-old monolith into services, but there's no clear agreement on where the seams should be, and a full rewrite isn't an option — the business still needs new features shipped weekly.",
    "solution": "Resist the pressure to design the target microservice map up front from a whiteboard — instead, identify seams from evidence: which modules change together in commits (a sign they're already coupled), which have genuinely independent scaling or team-ownership needs, and which have the clearest, most stable data contracts today.\n\nUse the strangler fig pattern: stand up new functionality as services in front of or alongside the monolith via a routing layer, and only extract existing monolith code when a real business need (a scaling bottleneck, a team boundary, a reliability isolation requirement) justifies the extraction cost. This keeps feature delivery unblocked while the decomposition happens incrementally and is validated against real usage rather than a speculative diagram."
  },
  {
    "id": "e-po-001",
    "role": "Product Owner",
    "problem": "Engineering estimates a highly requested feature at six weeks. A key customer is threatening to churn unless it ships in two. The engineering team says two weeks is only possible by cutting corners that will likely cause production issues.",
    "solution": "Don't treat this as a single yes/no negotiation on the deadline — decompose the feature and ask engineering what a genuinely reduced-scope version could look like in two weeks, versus what corners would actually be cut to hit two weeks at full scope.\n\nBring the customer a real choice: a narrower version of the feature that ships in two weeks and fully covers their immediate need, versus the full feature in six weeks. This is usually a better outcome than forcing engineering to ship the full scope unsafely, which risks the same customer churning anyway when it breaks in production."
  },
  {
    "id": "e-iam-001",
    "role": "IAM Engineer",
    "problem": "A large enterprise customer is onboarding onto your SaaS product and requires three things at once: (1) their employees must SSO into your app through their own corporate OIDC identity provider, (2) user and group accounts in your app must be automatically created, updated, and deactivated in sync with their internal directory — their IT team refuses to manage accounts manually in yet another admin console, and (3) your company already uses Auth0 as its IAM platform in front of the actual application. Design the end-to-end flow: how does a new employee get access on day one, how do federated login and directory sync actually relate to each other (they are not the same mechanism), how do the customer's directory groups become permissions in your app, and — critically — what happens the moment the customer's IT team deactivates a departing employee?",
    "solution": "Treat this as two separate, parallel flows that people new to IAM often conflate — federated login and directory provisioning solve different problems and both are needed.\n\n**Federation (OIDC):** register the customer's IdP as an Auth0 Enterprise Connection scoped to an Auth0 Organization representing that customer. On login, the app redirects to Auth0, Auth0 redirects to the customer's IdP, the employee authenticates with their own corporate credentials, and the IdP returns a signed ID token that Auth0 validates against the IdP's published JWKS (checking issuer/audience) before minting its own tokens for your app. This only answers \"who is this person right now\" — it says nothing about accounts that were never logged into, or accounts that need to be cut off before their next login.\n\n**Provisioning (SCIM):** the customer's directory (e.g. Entra ID/Okta) is configured to push SCIM `POST`/`PATCH`/deactivate calls to Auth0's (or your own) SCIM endpoint whenever a user or group changes on their side — independent of anyone logging in. This is what lets a new hire's account exist in your system before their first login (so day-one access isn't blocked on provisioning lag), and — more importantly — what lets a departure be acted on immediately rather than waiting for a login event that will never come.\n\n**Group-to-role mapping:** don't pass the customer's raw directory group names into your authorization model directly — map them in an Auth0 Action at token-issuance time (or in your SCIM group-sync handler) to your app's own internal roles/permissions. This decouples your authorization model from however each customer happens to name their internal groups, and lets you change your permission model without renegotiating with every customer's IT team.\n\n**Deprovisioning — the part most implementations get wrong:** when the customer deactivates the employee, SCIM fires a deactivation call. It's not enough to flip an `active: false` flag on the user record — you must also revoke that user's live sessions and outstanding refresh tokens immediately. If you only update the record, the employee keeps working with their existing access/refresh tokens until they naturally expire, which for long refresh-token lifetimes can mean days of retained access after termination. This gap — SCIM wired to the data record but not to active session/token revocation — is one of the most common findings in real IAM security reviews, and is exactly the kind of thing worth calling out explicitly in a design answer even if not asked directly."
  },
  {
    "id": "e-po-002",
    "role": "Product Owner",
    "problem": "Analytics show a shipped feature has very low adoption three months after launch, despite it having been the top item on the roadmap and taking a full quarter to build.",
    "solution": "Investigate before concluding the feature itself was the wrong bet — separate discoverability (do users even know it exists?), usability (do they try it and abandon it?), and value fit (do they use it and get no benefit?) as distinct hypotheses, since each has a different fix.\n\nInstrument the funnel specifically for this feature if it isn't already, talk directly to a handful of users who fit the target segment but haven't used it, and use those findings to decide whether to invest in exposure/onboarding, iterate on the UX, or accept the feature was a wrong bet and deprioritize further investment — rather than defaulting to \"add more visibility\" as a reflex fix."
  },
  {
    "id": "e-support-001",
    "role": "Application Support Engineer",
    "problem": "It's 2am and your on-call phone goes off: the payment service is returning 500s for roughly 30% of checkout requests, and the error rate is climbing. Customers are actively complaining on social media. You have no prior context on this incident. Walk through exactly what you do in the first 15 minutes.",
    "solution": "Acknowledge the page immediately so the alert doesn't escalate further, then spend the first couple of minutes purely on impact assessment: pull up the dashboard for the payment service, confirm the error rate and whether it's climbing or plateauing, and check if it correlates with a recent deployment, config change, or a specific downstream dependency (a quick check of recent deploys/changes is usually the fastest lead).\n\nPrioritize mitigation over full diagnosis — if a deployment in the last hour correlates with the spike, roll it back immediately rather than trying to root-cause it live; if not, check the most likely shared dependency (database, payment gateway, auth service) for its own health signals. Post a brief status update to the incident channel as soon as you have any signal, even \"investigating, error rate climbing, checking recent deploys\" — this keeps stakeholders informed without waiting for a full explanation, and escalate to a second responder immediately if you don't have a clear lead within the first 10 minutes rather than struggling alone while impact grows."
  },
  {
    "id": "e-support-002",
    "role": "Application Support Engineer",
    "problem": "A specific customer reports that exporting their monthly invoice to PDF has failed intermittently for the past two weeks, roughly once every 50 attempts, but you cannot reproduce it in any lower environment. How do you investigate and support this without introducing risky changes to a stable production system?",
    "solution": "Start by gathering everything observable from the actual failures rather than trying to force a reproduction blind: correlation IDs or timestamps of the failed attempts, whether they cluster around a particular time of day or coincide with other batch/background jobs running, and whether the failures are unique to this customer's data (an unusually large invoice, a special character in an address field, a specific product line) or genuinely random.\n\nSince it won't reproduce in lower environments, the most likely causes are either production-scale data triggering an edge case, or resource contention that only manifests under real concurrent load — both of which point toward adding targeted, low-risk logging around the PDF generation path (input size, timing, any caught-but-swallowed exceptions) rather than guessing at a fix. Once the pattern is understood from the added logging, apply the narrowest fix that addresses the actual root cause, and keep the extra logging in place until you've confirmed the fix worked across several billing cycles rather than declaring victory after just one quiet week."
  },
  {
    "id": "e-support-003",
    "role": "Application Support Engineer",
    "problem": "Your organization relies on a third-party vendor CRM system you don't have source code access to. After the vendor pushed a routine update last night, several internal users report that saving a customer record silently fails with no error message about 5% of the time. The vendor's support queue has a 3-business-day SLA. How do you support this in the meantime, and what do you escalate to the vendor?",
    "solution": "First establish a clean, minimal reproduction — exact steps, the specific fields/data involved, and whether it's truly random or tied to a particular record type or field combination — since a vague \"sometimes saving fails\" ticket will sit at the bottom of any vendor's queue, while a precise, reproducible report with a clear failure rate gets triaged faster.\n\nWhile waiting on the vendor, give affected users a workaround if one exists (retrying often succeeds, or saving via a different path if the CRM offers one) and document it clearly so support tickets. Given the business impact — a 5% silent failure rate on customer records is a real risk of lost data — escalate past the standard 3-day SLA queue through your account manager or a designated emergency contact if your contract has one, explicitly framing the business impact (data loss risk, not just an inconvenience) rather than resubmitting the same generic ticket and waiting. Track it internally as a known issue with the workaround documented, so the next person who reports it doesn't have to rediscover the same thing."
  },
  {
    "id": "e-devops-001",
    "role": "DevOps Engineer",
    "problem": "Your CI pipeline's end-to-end test suite has become flaky — about 1 in 10 runs fails for reasons unrelated to the actual code change, and the team has started reflexively clicking 're-run' without investigating. A critical release is blocked because of this. What do you do immediately, and what do you do to fix it properly?",
    "solution": "For the immediate blocker, don't just keep blindly re-running the whole suite — re-run only the specific failing test in isolation to distinguish genuine flakiness from a real regression the change actually introduced, and if it's confirmed flaky, make an explicit, documented decision to proceed (with a manual verification of the affected area) rather than silently overriding a red pipeline, which trains the team that red is meaningless.\n\nThe proper fix is to stop tolerating flakiness as background noise: quarantine known-flaky tests out of the blocking gate immediately so they stop eroding trust in every other pipeline run, track them in a visible backlog rather than letting them fail silently forever, and dedicate real time to root-causing each one — common causes are timing assumptions (a race condition in the test itself, not the code), shared mutable state between tests, or an unmocked external dependency that's occasionally slow or unavailable. The team's habit of reflexively re-running without investigating is itself the bigger risk to fix, since it means a genuine regression could just as easily be waved through as \"probably flaky.\""
  },
  {
    "id": "e-devops-002",
    "role": "DevOps Engineer",
    "problem": "A production deployment this morning caused a partial outage — about 15% of requests failed for 20 minutes before someone noticed and manually rolled back. Post-incident, leadership asks: why did it take 20 minutes to notice, and how do we make sure this doesn't happen again?",
    "solution": "There are two distinct problems here, and they need two distinct fixes. The detection problem: 20 minutes of unnoticed failures means there was no automated alerting tied to the deployment itself — the fix is automated post-deploy smoke tests and health-check gating that catch a bad deployment within seconds to minutes, not relying on someone happening to notice a dashboard or a customer complaint.\n\nThe blast-radius problem: the deployment went to 100% of traffic at once, so 15% of requests failed for the full 20-minute window rather than being caught on a small slice first — the fix is a progressive rollout strategy (canary or blue-green) that exposes new code to a small percentage of traffic initially, with automated rollback triggered by a metrics threshold (error rate, latency) rather than requiring a human to notice and manually intervene. Presented together to leadership: \"we're adding automated detection and progressive rollout with automated rollback\" is a concrete, convincing answer to \"how do we make sure this doesn't happen again\" — a promise to \"be more careful\" is not."
  },
  {
    "id": "e-devops-003",
    "role": "DevOps Engineer",
    "problem": "You discover that an API key with production database access was accidentally printed to the CI pipeline's build logs for the past three weeks, and those logs are visible to anyone with read access to the CI project (a fairly broad group internally). What do you do, in what order?",
    "solution": "First, rotate the exposed credential immediately, before anything else — three weeks of exposure to a broad internal audience means it has to be treated as potentially compromised regardless of whether you find evidence of misuse, and every minute it stays valid is unnecessary risk.\n\nSecond, assess actual exposure: check database access/audit logs for that credential over the exposure window for any activity that doesn't match expected application behavior, and identify exactly who had read access to those CI logs during that time. Third, address the immediate technical cause — stop the key from being printed (most CI platforms auto-redact values registered as pipeline secrets, but not values embedded in a script's own `echo`/print statements, which is a common way this leaks despite the platform's redaction). Fourth, fix the systemic gap: scan historical pipeline logs for other secret patterns that may have leaked the same way, review whether that pipeline's service connection had broader database access than it actually needed (least privilege would have limited the blast radius even if the leak still happened), and only after containment and root-cause fixes are done, write up the incident and notify whoever your organization's policy requires for a credential exposure of this kind."
  },
  {
    "id": "e-cloud-001",
    "role": "Cloud Engineer",
    "problem": "The business asks you to design a disaster recovery approach for a business-critical Azure App Service + Azure SQL Database application. They tell you: 'We can't lose more than 5 minutes of data, and we need to be back up within 15 minutes of a regional outage.' What do you propose, and what does it cost them?",
    "solution": "Translating the requirement: an RPO of 5 minutes and RTO of 15 minutes is aggressive enough that periodic backups alone won't meet it — this needs an actively running, kept-in-sync secondary region (a hot or warm standby), not a cold recovery-from-backup approach.\n\nFor the database, an Azure SQL auto-failover group replicates to a secondary region with a typically small replication lag, giving a realistic shot at the RPO target, though it's worth being explicit with the business that geo-replication is asynchronous and the actual RPO under a true regional failure depends on replication lag at the moment of failure, not a hard guarantee. For the App Service, deploy the application to both regions (via the same CI/CD pipeline targeting both, kept continuously in sync rather than deployed ad hoc), and use Azure Front Door or Traffic Manager for automated health-check-based failover routing. This comes at real cost — effectively running duplicate infrastructure continuously in a second region, plus cross-region data transfer — so it's worth confirming with the business whether that aggressive SLA genuinely applies to the whole system or just its most critical subset, and scheduling regular DR failover drills, since an untested failover plan often doesn't actually meet its stated RTO the first time it's really needed."
  },
  {
    "id": "e-cloud-002",
    "role": "Cloud Engineer",
    "problem": "Finance flags that the Azure bill for your team's environment has tripled over the past two months with no obvious change in usage patterns or new features shipped. You're asked to investigate and bring a plan to reduce cost without asking anyone to change their day-to-day workflow yet. How do you approach it?",
    "solution": "Start in Azure Cost Management, breaking down spend by resource and tag over the relevant period to find exactly what grew — a tripled bill with \"no obvious change\" almost always has a specific, findable cause once you look at cost by resource rather than the aggregate total.\n\nCommon culprits worth checking first: non-production environments (dev/test) left running 24/7 rather than scheduled to shut down outside business hours, orphaned resources (disks, public IPs, snapshots) left behind from deleted VMs that still incur cost, an App Service plan or database tier that was sized once and never revisited as load changed, or logging/telemetry (Log Analytics ingestion) left on a verbose setting that's silently ingesting and billing far more data than needed. Quick wins typically come from auto-scheduling non-prod resources and right-sizing anything clearly over-provisioned relative to its actual utilization metrics — and to prevent this recurring invisibly, set up budgets and cost alerts and enforce consistent tagging so a similar spike is caught and attributed quickly next time, rather than surfacing two months later via a finance report."
  },
  {
    "id": "e-cloud-003",
    "role": "Cloud Engineer",
    "problem": "A Logic App that syncs orders from your cloud CRM to an on-premises ERP system fails intermittently, roughly a dozen times a day, always with a generic timeout error. The on-prem team says their system is healthy. How do you diagnose and fix this?",
    "solution": "Since the call routes through the On-Premises Data Gateway, start there rather than assuming the on-prem ERP itself is the bottleneck — check the gateway machine's own resource utilization (CPU, memory) and whether it's a single gateway instance handling all traffic, since a gateway under load can itself become the source of timeouts even when the ultimate on-prem system is healthy; if volume is the issue, a high-availability gateway cluster with multiple instances spreads that load.\n\nAlso check whether the Logic App action's configured timeout is simply too short relative to the ERP's actual response time under its own peak load — correlate the dozen daily failures against the on-prem system's load pattern (a nightly batch job or backup window, for instance) to see if they cluster predictably rather than being truly random. Add a retry policy with exponential backoff for what are likely transient timeouts, and add an explicit error-handling branch that logs full failure details (not just \"timeout\") so future occurrences are faster to diagnose. If the volume or on-prem system's responsiveness genuinely can't reliably meet a synchronous call's timeout, consider redesigning the integration around a queue-based, asynchronous pattern instead, so a brief on-prem slowdown doesn't directly fail the sync."
  },
  {
    "id": "e-sec-001",
    "role": "Security Engineer",
    "problem": "A penetration test report comes back flagging a feature that lets users provide a URL for the app to fetch a preview image from (like a link-preview feature in a chat app) as vulnerable to SSRF, including a proof-of-concept where the tester retrieved data from the internal cloud metadata endpoint. You need to both fix this specific feature and figure out if similar features exist elsewhere in the codebase. What's your approach?",
    "solution": "For the flagged feature, block requests to private/internal IP ranges (including the cloud metadata endpoint's specific address) before the server makes the outbound fetch, and — critically — resolve the URL's DNS first and validate the resolved IP, not just the hostname string, since a naive hostname-only check can be bypassed via DNS rebinding (a domain that resolves to an internal IP). Also disallow following redirects blindly, since a URL that passes validation could redirect to an internal address after the check has already happened.\n\nAs defense in depth, route this kind of outbound \"fetch a user-supplied URL\" traffic through a locked-down network egress path that can't reach internal ranges at all, regardless of what the application-level validation does — so a bug in the validation logic isn't the only thing standing between an attacker and internal resources. Then broaden the investigation: search the codebase for any other feature that accepts a user-supplied URL the server later fetches (webhooks, \"import from URL\" features, avatar-from-URL uploads), since these tend to be implemented independently by different features over time without a shared safeguard — build one shared, vetted \"safe fetch\" utility that encapsulates this protection, and require new URL-fetching features to use it rather than relying on each developer remembering to reimplement the same checks correctly."
  },
  {
    "id": "e-sec-002",
    "role": "Security Engineer",
    "problem": "A critical CVE is announced for a logging library used transitively by roughly 40 microservices across your organization. Exploitation requires no authentication and is already being actively exploited in the wild. You have limited patching capacity. How do you triage and respond across the organization in the next 24 hours?",
    "solution": "Triage by actual exploitable exposure, not just presence of the library — first identify which of the 40 services are internet-facing and reachable by an unauthenticated attacker versus purely internal, since that's the dimension that matters most given no-auth exploitation. Within that internet-facing set, further narrow to which services actually invoke the specific vulnerable code path, since not every usage of a library triggers the vulnerable function.\n\nPatch or mitigate that highest-risk subset first within the 24-hour window — upgrade to a fixed version where available, or apply a documented interim mitigation (a config flag, a WAF rule blocking the known exploit pattern) if a full patch and redeploy can't happen fast enough for every affected service. Track status centrally in one shared place so leadership and other responders see real-time progress across all 40 services rather than each team reporting independently and creating a fog of uncertain status. Only after the highest-risk services are covered, work through the remaining internal-only services on a slightly longer but still prioritized timeline — and once the fire is out, the natural follow-up is asking why discovering \"which of our 40 services use this library, and how\" took as long as it did, since a maintained software bill of materials (SBOM) or centralized dependency inventory would have made this exact triage dramatically faster next time."
  },
  {
    "id": "e-sec-003",
    "role": "Security Engineer",
    "problem": "Your monitoring flags an unusual pattern: over the last hour, one account has had over 200 failed login attempts followed by a successful login from a new, unrecognized location, and shortly after, the account changed its own registered email address. What do you do, and how do you tell whether this is a real compromise or a false alarm?",
    "solution": "Treat this as a likely account takeover pending investigation, and contain first rather than waiting for certainty — force-logout all active sessions for the account and require re-verification before any further sensitive action can be taken, since 200 failed attempts followed by a success is a classic credential-stuffing signature, and the subsequent email change is a common attacker move specifically intended to lock the real owner out of account recovery.\n\nVerify through an out-of-band channel — contact the account owner via a method not controlled by the compromised email (phone, a known secondary contact) rather than trusting the current account state, since the changed email itself can't be trusted as a channel at this point. If MFA wasn't enabled on the account, that's very likely the actual root cause enabling this, not just an unlucky password guess — and if confirmed as a real compromise, don't treat it as an isolated single-account incident: check whether the same source IP or attack pattern hit other accounts around the same time, since credential-stuffing attacks are almost always run against many accounts at once, and consider proactively forcing password resets for any other accounts showing the same failed-then-succeeded pattern. Longer term, this is the concrete case to make for enforcing MFA and adding rate-limiting/lockout after repeated failed attempts, if either isn't already in place."
  },
  {
    "id": "e-qa-001",
    "role": "QA Engineer",
    "problem": "Your team's full test suite (unit + integration + end-to-end) takes just over 3 hours to run, and it's now the single biggest bottleneck to shipping — a release that's ready at 9am doesn't actually go out until after lunch because everyone's waiting on the pipeline. Leadership wants this down to under 30 minutes without meaningfully reducing coverage. What's your plan?",
    "solution": "Start by measuring where the 3 hours actually goes, broken down by unit, integration, and end-to-end — it's very common for a comparatively small number of slow end-to-end tests to dominate total runtime despite the unit test layer having far more individual tests, simply because each E2E test is slow and they're often run serially.\n\nParallelize aggressively first, since it's the fastest lever with no coverage tradeoff — shard the suite across multiple CI workers running concurrently, which most CI platforms support directly. Then restructure what blocks a release versus what runs in the background: the fast unit-test layer should gate every commit/PR since it's cheap, but the full, slow E2E suite doesn't need to block every single PR — a small, curated \"smoke\" subset of the most critical E2E flows can gate PRs, while the complete E2E suite runs on merges to main or on a schedule instead. Finally, actively hunt down and fix individually slow tests (unnecessary sleeps, unmocked network calls, unnecessarily heavy test data setup) rather than just parallelizing around a poorly-written slow test — parallelization multiplies the benefit of a fast test but can only do so much to hide a fundamentally slow one."
  },
  {
    "id": "e-qa-002",
    "role": "QA Engineer",
    "problem": "A bug that corrupted a subset of customer records slipped through to production despite the release passing all automated tests and a full manual QA sign-off. In the postmortem, how do you investigate why the existing test strategy missed it, without it turning into a blame session against whoever signed off?",
    "solution": "Frame the investigation around the coverage gap itself, not who approved the release — reconstruct precisely what input or condition triggered the corruption, then ask concretely: was there ever a test covering this specific scenario at all (a genuine edge case nobody had considered), was there a test that should have caught it but had a flaw of its own (a weak or incorrect assertion that passed when it shouldn't have), or was the scenario only reproducible under production-scale data or concurrency that no test environment realistically represents.\n\nEach of those three answers points to a different, concrete fix — add the missing edge-case test, fix the specific weak assertion, or invest in production-scale/load-representative testing or a safer progressive rollout that would have limited the blast radius even if the bug wasn't caught beforehand. Keeping the postmortem blameless here isn't just about being kind — it's functionally necessary: if the conclusion becomes \"the QA sign-off should have caught this,\" people start being less candid about what they did and didn't actually verify in future sign-offs, which makes the next gap harder to find, not easier."
  },
  {
    "id": "e-dba-001",
    "role": "DBA",
    "problem": "During peak business hours, users report the application is 'frozen' when saving orders. You check SQL Server and see dozens of sessions in a blocked state, all waiting on the same table. The blocking session itself doesn't appear to be doing anything wrong on its own. How do you diagnose and resolve this without taking the system offline?",
    "solution": "Use `sys.dm_exec_requests` and `sys.dm_tran_locks` (or `sp_who2` as a quicker first look) to identify the actual head blocker and exactly what resource it's holding, then look at what that session is running — a very common root cause is a long-running transaction (a reporting query, a batch job, or application code that opened a transaction and fetched more data than intended before committing) holding locks far longer than the workload actually requires.\n\nAs an immediate, careful mitigation, that specific blocking session can be killed if it's safe to do so (understanding its work will roll back) — but the durable fix is addressing why it held locks that long in the first place: check whether a missing index is causing a scan that widens the locked range unnecessarily, whether the transaction should be broken into smaller units that commit more frequently, or whether switching to READ COMMITTED SNAPSHOT isolation (RCSI) would eliminate most reader-writer blocking for this specific workload without requiring any application code changes."
  },
  {
    "id": "e-dba-002",
    "role": "DBA",
    "problem": "The application team needs to add a NOT NULL column to a table with 200 million rows that is under continuous read and write load, and they've asked you whether it's safe to just run the ALTER TABLE statement during business hours. What do you tell them, and what do you actually do?",
    "solution": "The honest answer is \"it depends,\" and it's worth explaining why rather than giving a flat yes or no: since SQL Server 2012, adding a NOT NULL column with a default that's a runtime constant is a fast, metadata-only operation that doesn't rewrite the table, and is generally safe to run even under load. If the default isn't a runtime constant, or the engine version is older, a direct ALTER would attempt to physically rewrite all 200 million rows under a blocking schema lock — on an actively-used table that size, this would very likely cause a serious, business-impacting outage rather than a quick change.\n\nFor the cases where the fast path doesn't apply, the safer approach is: add the column as nullable first (a fast, low-risk operation on its own), backfill the value in small batches with brief pauses between them to avoid sustained lock pressure and excessive transaction log growth, and only add the actual `NOT NULL` constraint once every row already has a value — at which point enforcing the constraint is a much cheaper validation rather than a full rewrite. Regardless of which path applies, test the exact operation's real-world timing against a production-scale copy first rather than assuming, and schedule it outside peak hours if there's any remaining uncertainty, with a clear rollback/abort plan defined before starting."
  }
]
