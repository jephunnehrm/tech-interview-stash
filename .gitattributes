# Normalize line endings to LF for all text files, regardless of the
# checkout platform's default — avoids the "LF will be replaced by CRLF"
# warnings on Windows and keeps diffs clean across contributors' OSes.
* text=auto eol=lf
